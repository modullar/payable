# payable
simple nodejs app

# Setup:
 - Clone the repo
 - `npm install`
 - `sudo mongod start`
 - `npm run dev`

Try `REST` actions on `payments` endpoint after running the server using `Postman` or `Paw`.
